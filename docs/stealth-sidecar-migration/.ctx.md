# Context Snapshot: Stealth Sidecar Migration

## Project shape
apps, config.example.yaml, CONTEXT.md, docker-compose.yml, Dockerfile, docs, eslint, grafana, LICENSE, loki, Makefile, package.json, packages, playwright.config.ts, pnpm-lock.yaml, pnpm-workspace.yaml, promtail, README.md, scripts, services, spec, tests, tsconfig.base.json, tsconfig.test.json, turbo.json

pnpm workspaces + Turborepo monorepo: packages/shared (@fm/shared — Zod schemas/DTOs), packages/core (@fm/core — pipeline, platforms, storage+migrations, analytics, llm, alerts), packages/api (@fm/api — Fastify JSON API + dashboard server), apps/web (@fm/web — React SPA), apps/cli (@fm/cli — run/report/dashboard entrypoints). Dependency graph: shared ← core, api, web, cli; core ← api, cli; api ← cli; web → shared only.

## CLAUDE.md
# fashion-monitor — execution routing

Cross-cutting home-lab conventions (service users, Mac-vs-desktop execution, ollama
broker, secrets, commit attribution, scraping/egress, shared-service-vs-library) live in
home-infra/CONVENTIONS.md. This file holds only what's fashion-monitor-specific.

Deploy target is the desktop, not this Mac checkout. Migrated off the NAS on 2026-07-19.
Images are built here and shipped — this repo never builds on the deploy host itself:

    make sync   # compose file, static config, grafana provisioning -> deploy host
    make push   # docker save fashion-monitor/cli fashion-monitor/mcp-server | ssh ... docker load
    make deploy # sync + push, then docker compose up -d on the deploy host

- Deployed/running copy: desktop Docker Compose stack, dedicated `fashionmonitor`
  service user, `/opt/docker/fashion-monitor`.
- Live data: the desktop-mounted SQLite DB + `data/poshmark-profile` (persistent
  Playwright cookies) — never present on this Mac checkout.
- Scrape/score split (2026-07-19): two separate CLI entrypoints (apps/cli/src/scrape.ts,
  apps/cli/src/score.ts). `scraper`/`poshmark` compose services attach to home-infra's
  `scraper-egress` VPN tunnel (network_mode: container:gluetun-scraper) and never touch
  the LLM; `score` stays off the tunnel to reach the LAN Ollama broker. `gluetun-scraper`
  (separate compose project) must already be running before `scraper`/`poshmark` start.
- Live scrapes are opt-in only (pnpm run verify:scrapers, pnpm run test:live) — default
  dev/test loop runs entirely against fixtures. Don't run live scrapers from this Mac
  clone casually; they hit real marketplaces.

## CONTEXT.md
Domain glossary: Monitor, Taste, User, Invite, Profile, Role, Score (YES/MAYBE/NO),
Query Override, Secret, Connection (per-profile per-platform link: API-key/none/login —
login-based e.g. Poshmark/Depop violates ToS, ban risk, off by default; falls back to
anonymous public scraping when not connected), Pipeline (scrapes, dedupes, prefilters,
scores, alerts on schedule), Scoring Dimensions, Feedback.

## ADRs (titles only)
- 0001-mcp-as-primary-interface.md
- 0002-secrets-encrypted-in-db.md
- 0003-invite-only-self-service-profiles.md
- 0004-tiered-connections-login-dormant.md
- 0005-multi-profile-serial-pipeline.md — one tick iterates active profiles serially
- 0006-inference-via-shared-gpu-broker.md — inference goes through a shared GPU broker, not direct Ollama calls
- 0007-ntfy-over-telegram-for-push-alerts.md — push alerts are delivered via self-hosted ntfy, not Telegram

## Related prior spec (superseded artifact, not this feature's own history)
docs/patchright-stealth-driver/ — the original spec that built PR #5's in-process
TS Patchright driver (packages/core/src/platforms/playwright/browser.ts). This feature
retires that artifact; do not confuse its TASKS.md/challenge-notes.md with this feature's.

## Cross-repo dependency (flagged in this feature's own plan.md Risk Areas)
scraper-commons' sidecar (src/scraper_commons/sidecar/) is built and merged to main as
of 2026-07-21, but has no Dockerfile/container image yet. This feature's docker-compose.yml
step documents the attach contract but the image itself is an external prerequisite —
see plan.md Risk Area #1.
